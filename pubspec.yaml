name: siberian_core
description: Core package
version: 1.0.3
publish_to: 'none'

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  logger: ^1.1.0
  sprintf: ^7.0.0
  uuid: ^3.0.6
  provider: ^6.0.3
  flutter_bloc: ^8.1.1
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
