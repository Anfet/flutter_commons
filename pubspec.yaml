name: siberian_core
description: Core package
version: 1.0.8
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  logger: ^2.0.2+1
  sprintf: ^7.0.0
  uuid: ^3.0.7
  short_uuids: ^2.0.0
  provider: ^6.1.1
  flutter_bloc: ^8.1.3
  intl: any

  siberian_logger:
    git:
      url: https://git.siberian.pro/toplionkin/flutter_siberian_logger.git
      ref: 'master'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
