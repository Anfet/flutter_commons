name: flutter_commons
description: Core package
version: 1.0.17+9
publish_to: 'none'
homepage: https://github.com/Anfet/flutter_commons.git

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  flutter_bloc: ^8.1.6
  stream_transform: ^2.1.0
  bloc_concurrency: ^0.2.5

  flutter_logger:
    git:
      url: https://github.com/Anfet/flutter_logger.git
      ref: 'release/0.0.3'

  sprintf: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any
 
